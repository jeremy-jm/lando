name: Build and Release

on:
  push:
    branches:
      - master
  workflow_dispatch: # Allow manual triggering

jobs:
  build-linux:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.6'
          channel: 'stable'
          cache: true

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang \
            cmake \
            ninja-build \
            pkg-config \
            libgtk-3-dev \
            libgstreamer1.0-dev \
            libgstreamer-plugins-base1.0-dev \
            liblzma-dev \
            libstdc++-12-dev \
            fakeroot \
            libkeybinder-3.0-dev \
            gir1.2-keybinder-3.0

      - name: Install dependencies
        run: flutter pub get

      - name: Generate localization files
        run: flutter gen-l10n || true || true

      - name: Get version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build Linux
        run: flutter build linux --release

      - name: Create Linux TAR.GZ
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cd build/linux/x64/release/bundle
          tar -czf ../../../lando-linux-x64-linux-v${VERSION}.tar.gz .
          mv ../../../lando-linux-x64-linux-v${VERSION}.tar.gz build/linux/

      - name: Create Linux DEB package
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mkdir -p deb-package/DEBIAN
          mkdir -p deb-package/usr/bin
          mkdir -p deb-package/usr/share/applications
          
          # Copy files
          cp -r build/linux/x64/release/bundle/* deb-package/usr/bin/
          
          # Create control file
          cat > deb-package/DEBIAN/control <<EOF
          Package: lando
          Version: $VERSION
          Section: utils
          Priority: optional
          Architecture: amd64
          Maintainer: Lando Team
          Description: A translation software that uses third-party services for word lookup
          EOF
          
          # Create desktop file
          cat > deb-package/usr/share/applications/lando.desktop <<EOF
          [Desktop Entry]
          Version=$VERSION
          Type=Application
          Name=Lando Dictionary
          Comment=A translation software
          Exec=/usr/bin/lando
          Icon=lando
          Terminal=false
          Categories=Utility;
          EOF
          
          # Build DEB
          fakeroot dpkg-deb --build deb-package build/linux/lando-linux-amd64-linux-v${VERSION}.deb

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: |
            build/linux/lando-linux-x64-linux-v${{ steps.version.outputs.version }}.tar.gz
            build/linux/lando-linux-amd64-linux-v${{ steps.version.outputs.version }}.deb
          if-no-files-found: warn

  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.6'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate localization files
        run: flutter gen-l10n || true || true

      - name: Get version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build Windows
        run: flutter build windows --release

      - name: Create Windows ZIP
        shell: powershell
        run: |
          $VERSION = "${{ steps.version.outputs.version }}"
          Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath build\windows\lando-windows-x64-windows-v${VERSION}.zip -Force

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: build/windows/lando-windows-x64-windows-v${{ steps.version.outputs.version }}.zip
          if-no-files-found: warn

  build-macos:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.6'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate localization files
        run: flutter gen-l10n || true || true

      - name: Get version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build macOS
        run: flutter build macos --release

      - name: Create macOS ZIP
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cd build/macos/Build/Products/Release
          zip -r ../../../lando-macos-macos-v${VERSION}.zip lando.app

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create macOS DMG
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cd build/macos/Build/Products/Release
          create-dmg \
            --volname "Lando Dictionary" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --icon "lando.app" 200 190 \
            --hide-extension "lando.app" \
            --app-drop-link 600 190 \
            "../../../lando-macos-macos-v${VERSION}.dmg" \
            "lando.app"

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: |
            build/macos/lando-macos-macos-v${{ steps.version.outputs.version }}.zip
            build/macos/lando-macos-macos-v${{ steps.version.outputs.version }}.dmg
          if-no-files-found: warn

  build-android:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate localization files
        run: flutter gen-l10n || true || true

      - name: Get version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Build Android APK
        run: flutter build apk --release

      - name: Build Android App Bundle
        run: flutter build appbundle --release

      - name: Rename Android artifacts
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cp build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/app-release-android-v${VERSION}.apk
          cp build/app/outputs/bundle/release/app-release.aab build/app/outputs/bundle/release/app-release-android-v${VERSION}.aab

      - name: Upload Android artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-artifacts
          path: |
            build/app/outputs/flutter-apk/app-release-android-v${{ steps.version.outputs.version }}.apk
            build/app/outputs/bundle/release/app-release-android-v${{ steps.version.outputs.version }}.aab
          if-no-files-found: warn

  release:
    needs: [build-linux, build-windows, build-macos, build-android]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: List artifacts
        run: |
          echo "Checking artifacts..."
          find artifacts -type f || true
          ls -la artifacts/ || true
          echo "Linux artifacts:"
          ls -la artifacts/linux-artifacts/ || echo "No Linux artifacts found"
          echo "Windows artifacts:"
          ls -la artifacts/windows-artifacts/ || echo "No Windows artifacts found"
          echo "macOS artifacts:"
          ls -la artifacts/macos-artifacts/ || echo "No macOS artifacts found"
          echo "Android artifacts:"
          ls -la artifacts/android-artifacts/ || echo "No Android artifacts found"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: Release v${{ steps.version.outputs.version }}
          body: |
            ## Release v${{ steps.version.outputs.version }}
            
            ### Downloads
            
            - **macOS**: [DMG](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/lando-macos-macos-v${{ steps.version.outputs.version }}.dmg) | [ZIP](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/lando-macos-macos-v${{ steps.version.outputs.version }}.zip)
            - **Windows**: [ZIP](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/lando-windows-x64-windows-v${{ steps.version.outputs.version }}.zip)
            - **Linux**: [DEB](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/lando-linux-amd64-linux-v${{ steps.version.outputs.version }}.deb) | [TAR.GZ](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/lando-linux-x64-linux-v${{ steps.version.outputs.version }}.tar.gz)
            - **Android**: [APK](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/app-release-android-v${{ steps.version.outputs.version }}.apk) | [AAB](https://github.com/${{ github.repository }}/releases/download/v${{ steps.version.outputs.version }}/app-release-android-v${{ steps.version.outputs.version }}.aab)
            
            ### Changes
            See commit history for details.
          files: |
            artifacts/linux-artifacts/*
            artifacts/windows-artifacts/*
            artifacts/macos-artifacts/*
            artifacts/android-artifacts/*
          fail_on_unmatched_files: false
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
